version: "3.8"

services:
  postgres-portal:
    image: postgres:16-alpine
    container_name: postgres-portal
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - portal_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - portal-internal

  redis-portal:
    image: redis:7-alpine
    container_name: redis-portal
    restart: always
    volumes:
      - portal_redis_data:/data
    networks:
      - portal-internal

  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portal-edupay-web
    restart: always
    depends_on:
      postgres-portal:
        condition: service_healthy
      redis-portal:
        condition: service_started
    environment:
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NEXTAUTH_URL: ${NEXTAUTH_URL}
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET}
      WEBPAY_ENVIRONMENT: ${WEBPAY_ENVIRONMENT}
      WEBPAY_COMMERCE_CODE: ${WEBPAY_COMMERCE_CODE}
      WEBPAY_API_KEY: ${WEBPAY_API_KEY}
      EDUPAY_API_URL: ${EDUPAY_API_URL}
      EDUPAY_API_TOKEN: ${EDUPAY_API_TOKEN}
      NEXT_PUBLIC_TENANT_ID: ${NEXT_PUBLIC_TENANT_ID}
      CRON_SECRET: ${CRON_SECRET}
      EMAIL_FROM: ${EMAIL_FROM}
      RESEND_API_KEY: ${RESEND_API_KEY:-}
      RESEND_FROM_EMAIL: ${RESEND_FROM_EMAIL}
      REDIS_URL: ${REDIS_URL:-redis://redis-portal:6379}
    command: >
      sh -c "npx prisma migrate deploy && node server.js"
    networks:
      - portal-internal
      - coolify

volumes:
  portal_pg_data:
  portal_redis_data:

networks:
  portal-internal:
    name: portal-edupay-internal
  coolify:
    external: true
